package app.grapheneos.setupwizard.action

import android.app.Activity

// Stub
object WifiActions {
    fun launchSetup(activity: Activity) {
        // In reality, it calls startActivityForResult() for a system WiFi setup activity
        // So the result is delivered to onActivityResult()
    }
}
